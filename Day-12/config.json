{
    "database": {
        "host": "prod-db.example.com",
        "port": 5432,
        "db_user": "admin",
        "db_password": "securepassword"
    },

    "api": {
        "endpoint": "https://api.example.com",
        "timeout": 30
    }
}